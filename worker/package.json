{
  "name": "fridge-chef-worker",
  "version": "1.0.0",
  "private": true,
  "description": "AI ingredient-detection backend for Fridge Chef. Holds one API key (OpenAI or Anthropic) so the public web app doesn't have to.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
